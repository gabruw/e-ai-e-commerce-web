//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles({
    loadingIcon: {
        margin: '4px 10px 0 0 '
    },
    dropDownIcon: {
        margin: '5px 10px 0 0'
    }
});

export default useStyles;
