//#region Imports

import STATE_FIELD from 'utils/constants/field/state';
import STATE_LABEL from 'utils/constants/label/state';

//#endregion

const STATE_COLUMN = [
    { label: STATE_LABEL.THIS, field: STATE_FIELD.NAME },
    { label: STATE_LABEL.COUNTRY, field: STATE_FIELD.COUNTRY }
];

export default STATE_COLUMN;
